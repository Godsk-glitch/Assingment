Laravel

//To create a new project in Laravel using composer

composer create-project laravel/laravel example-app 
cd example-app 
php artisan serve 
<------------------------------------------------------------------------------------------------->

//To create a component
php artisan make:compnent Header
//will be created under resources->view->component->header.blade.php

<------------------------------------------------------------------------------------------------->

//To create a controller
php artisan make:controller UserController
//will be created under app->HTTP->Controllers->userControllers.php

<------------------------------------------------------------------------------------------------->

//To make a Middleware
php artisan make:middleware ageCheck
//will be created under app->HTTP->MiddleWare->ageCheck.php

<------------------------------------------------------------------------------------------------->

//To make a model
php artisan make:model user

//always rememebr in case of model is lural to singular eh. users ---- user
// always capatalize the letter of the file
// will be created under app->Models->User.php

Note:-if file is already created theen delete it and create a new one.

<--------------------------------------------------------------------------------------------------->

